mkfifo /usr/local/elevate/elevate.fifo
while true; do
    if read -r cmd < /usr/local/elevate/elevate.fifo; then
        [[ -z "$cmd" ]] && continue
        echo "[elevate-daemon] running: $cmd"
        sudo bash -c "$cmd"
    fi
done
